tls:
  certificates:
    - certFile: /certs/mrbills.localhost.pem
      keyFile: /certs/mrbills.localhost-key.pem
  stores:
    default:
      defaultCertificate:
        certFile: /certs/mrbills.localhost.pem
        keyFile: /certs/mrbills.localhost-key.pem
